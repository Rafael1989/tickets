<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="2026-07-31-04" author="ticketwave">
        <comment>Schedules: a bookable departure/session of a route, with its own fare and status.</comment>
        <createTable tableName="schedules">
            <column name="schedule_id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" primaryKeyName="pk_schedules"/>
            </column>
            <column name="route_id" type="BIGINT">
                <constraints nullable="false"
                             foreignKeyName="fk_schedules_route"
                             references="routes(route_id)"/>
            </column>
            <column name="departure_time" type="TIMESTAMP WITH TIME ZONE">
                <constraints nullable="false"/>
            </column>
            <column name="arrival_time" type="TIMESTAMP WITH TIME ZONE">
                <constraints nullable="false"/>
            </column>
            <column name="base_fare" type="DECIMAL(12,2)">
                <constraints nullable="false"/>
            </column>
            <column name="currency" type="VARCHAR(3)" defaultValue="USD">
                <constraints nullable="false"/>
            </column>
            <column name="status" type="VARCHAR(20)" defaultValue="scheduled">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <sql dbms="postgresql">
            ALTER TABLE schedules ADD CONSTRAINT ck_schedules_status
                CHECK (status IN ('scheduled', 'delayed', 'cancelled', 'completed'));
        </sql>

        <createIndex tableName="schedules" indexName="ix_schedules_route_id">
            <column name="route_id"/>
        </createIndex>
        <createIndex tableName="schedules" indexName="ix_schedules_departure_time">
            <column name="departure_time"/>
        </createIndex>

        <rollback>
            <dropTable tableName="schedules"/>
        </rollback>
    </changeSet>

</databaseChangeLog>
