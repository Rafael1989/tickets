<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.0.7</version>
        <relativePath/>
    </parent>

    <groupId>com.ticketwave</groupId>
    <artifactId>ticketwave-backend</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <name>ticketwave-backend</name>
    <description>TicketWave ticket booking system backend</description>

    <properties>
        <java.version>21</java.version>
        <mapstruct.version>1.6.3</mapstruct.version>
        <lombok-mapstruct-binding.version>0.2.0</lombok-mapstruct-binding.version>
        <jjwt.version>0.12.6</jjwt.version>
        <springdoc.version>3.0.3</springdoc.version>
        <spring-cloud-contract.version>5.0.3</spring-cloud-contract.version>
        <!-- Spring Boot 4.0.6 does not manage org.testcontainers versions. -->
        <testcontainers.version>1.21.3</testcontainers.version>
        <!--
        Docker API version the *IT JVM talks; see the Failsafe configuration
        for why it has to be stated at all. Overridable so a machine with an
        older engine is not stuck editing the pom:

          ./mvnw verify -Ddocker.api.version=1.41

        1.44 is Docker Engine 25's. The floor is 1.40, below which Engine 26+
        refuses; the ceiling is whatever the local daemon supports, and Docker
        accepts any version at or under its own.
        -->
        <docker.api.version>1.44</docker.api.version>
    </properties>

    <dependencies>
        <!-- Web -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- Persistence -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
            <exclusions>
                <!--
                log4j-to-slf4j only exists to bridge callers of the Log4j2 API
                into SLF4J/Logback, the actual logging backend used here.
                Nothing in this project or its dependencies logs through the
                Log4j2 API, so the bridge is dead weight - and it drags in
                log4j-api@2.25.4, whose CVE-2026-49844 has no fixed release
                yet (`snyk test`). Dropping the bridge removes the vulnerable
                jar from the classpath entirely with no functional loss.
                -->
                <exclusion>
                    <groupId>org.apache.logging.log4j</groupId>
                    <artifactId>log4j-to-slf4j</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.liquibase</groupId>
            <artifactId>liquibase-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-liquibase</artifactId>
        </dependency>

        <!-- Validation -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- Security / JWT -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>${jjwt.version}</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>

        <!-- OpenAPI -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>${springdoc.version}</version>
        </dependency>

        <!-- Caching: see CacheConfig / ScheduleCatalogCache -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-cache</artifactId>
        </dependency>
        <dependency>
            <groupId>com.github.ben-manes.caffeine</groupId>
            <artifactId>caffeine</artifactId>
        </dependency>

        <!-- Mapping -->
        <dependency>
            <groupId>org.mapstruct</groupId>
            <artifactId>mapstruct</artifactId>
            <version>${mapstruct.version}</version>
        </dependency>

        <!-- Boilerplate reduction -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-webmvc-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-data-jpa-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-jdbc-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
        <!--
        Testcontainers: the *IT suite gets its own throwaway PostgreSQL per
        run (see PostgresContainerSupport), replacing the shared local
        database every IT used to share. Docker is now required for
        `mvn verify` - docs/testing.md says so up front, including the Docker
        Desktop API-version caveat on Windows.
        -->
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>postgresql</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Contract testing: see AuthContractBase and src/test/resources/contracts -->
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-contract-verifier</artifactId>
            <version>${spring-cloud-contract.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>testcontainers-bom</artifactId>
                <version>${testcontainers.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!--
            Explicit pins for transitive dependencies Snyk flagged that are not
            realigned by the spring-boot-starter-parent patch bump above (see
            `snyk test` output): postgresql driver, jjwt's Jackson 2.x, spring
            cloud contract's Jackson 3.x ("tools.jackson.core"), logback, and
            tomcat-embed. Child dependencyManagement wins over the parent BOM's
            managed versions, so these force the patched versions regardless
            of what 4.0.7 itself bundles.
            -->
            <dependency>
                <groupId>org.postgresql</groupId>
                <artifactId>postgresql</artifactId>
                <version>42.7.12</version>
            </dependency>
            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-databind</artifactId>
                <version>2.21.5</version>
            </dependency>
            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-core</artifactId>
                <version>2.21.5</version>
            </dependency>
            <dependency>
                <groupId>tools.jackson.core</groupId>
                <artifactId>jackson-databind</artifactId>
                <version>3.1.5</version>
            </dependency>
            <dependency>
                <groupId>tools.jackson.core</groupId>
                <artifactId>jackson-core</artifactId>
                <version>3.1.5</version>
            </dependency>
            <dependency>
                <groupId>ch.qos.logback</groupId>
                <artifactId>logback-core</artifactId>
                <version>1.5.36</version>
            </dependency>
            <dependency>
                <groupId>ch.qos.logback</groupId>
                <artifactId>logback-classic</artifactId>
                <version>1.5.36</version>
            </dependency>
            <dependency>
                <groupId>org.apache.tomcat.embed</groupId>
                <artifactId>tomcat-embed-core</artifactId>
                <version>11.0.23</version>
            </dependency>
            <dependency>
                <groupId>org.apache.tomcat.embed</groupId>
                <artifactId>tomcat-embed-websocket</artifactId>
                <version>11.0.23</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-contract-maven-plugin</artifactId>
                <version>${spring-cloud-contract.version}</version>
                <!--
                Deliberately NOT <extensions>true</extensions>. Binding the
                three goals by hand costs three lines and makes the lifecycle
                contribution visible in the file instead of implied by an
                extension realm. Each goal keeps its own default phase
                (generate-test-resources, generate-test-sources, package).

                This was first tried as a fix for the resolver's
                cross-ClassRealm ClassCastException (see .mvn/maven.config).
                It is not one: the plugin realm is created when the plugin
                executes, extension or not, and a cold-repository build with
                <extensions>true</extensions> gone fails identically. The
                plugin does supply the conflicting httpclient — it bundles
                maven-resolver-transport-http and httpclient 4.5.14 — but the
                fix for that is the transport selection in maven.config, not
                this. Kept on its own merits; recorded so nobody retests the
                hypothesis.
                -->
                <executions>
                    <execution>
                        <id>contracts</id>
                        <goals>
                            <goal>convert</goal>
                            <goal>generateTests</goal>
                            <goal>generateStubs</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <testFramework>JUNIT5</testFramework>
                    <!--
                    Without this, generated tests land under
                    org.springframework.cloud.contract.verifier.tests, and
                    Spring Boot's test context can't find
                    TicketwaveApplication by searching upward from there
                    ("Unable to find a @SpringBootConfiguration").
                    -->
                    <basePackageForTests>com.ticketwave.contract.generated</basePackageForTests>
                    <baseClassMappings>
                        <baseClassMapping>
                            <contractPackageRegex>.*auth.*</contractPackageRegex>
                            <baseClassFQN>com.ticketwave.contract.AuthContractBase</baseClassFQN>
                        </baseClassMapping>
                    </baseClassMappings>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
            <!--
            Previously missing entirely: the project's *IT classes (integration
            tests against a local PostgreSQL) were never actually wired into the
            build — surefire's default naming patterns don't match *IT, and with
            no failsafe execution bound, `mvn verify` silently skipped them.
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <configuration>
                    <!--
                    Required for the *IT run to be measured at all. Paired with
                    <propertyName>failsafeArgLine</propertyName> on jacoco's
                    prepare-agent-integration: by default that goal publishes
                    into ${argLine} - the same property prepare-agent already
                    set for surefire - so the two would depend on execution
                    order to not clobber each other. Naming them separately
                    makes which agent lands on which run explicit rather than
                    incidental.
                    -->
                    <argLine>${failsafeArgLine}</argLine>
                    <systemPropertyVariables>
                        <!--
                        Pins the Docker API version docker-java talks, for the
                        forked *IT JVM only (nothing under Surefire touches
                        Docker).

                        Without it, Testcontainers negotiates the connection
                        correctly - the log even prints "API Version: 1.55" -
                        but the image-pull command still goes out as client
                        version 1.32, and any Docker Engine from 26 onwards
                        answers "client version 1.32 is too old. Minimum
                        supported API version is 1.40". The symptom moves
                        around (sometimes the /info probe fails instead,
                        depending on whether ~/.testcontainers.properties has
                        cached a strategy), which makes it look like several
                        unrelated problems; it is one.

                        Note this is docker-java's own `api.version` property,
                        not the DOCKER_API_VERSION environment variable -
                        setting that one does not help, because docker-java
                        namespaces env vars differently.

                        The value lives in the docker.api.version property so
                        it can be overridden on the command line rather than by
                        editing this file.
                        -->
                        <api.version>${docker.api.version}</api.version>
                    </systemPropertyVariables>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>0.8.15</version>
                <configuration>
                    <!--
                    Coverage is measured on hand-written production code only.
                    MapStruct emits *MapperImpl into target/generated-sources
                    with a null-guard branch per property, and DevDataSeeder is
                    a @Profile("seed") dev-fixture runner that never executes
                    outside a developer's local database. Counting either one
                    buries the real gaps: together they accounted for ~65% of
                    all missed branches in the bundle.
                    -->
                    <excludes>
                        <exclude>**/*MapperImpl.class</exclude>
                        <exclude>com/ticketwave/devseed/**</exclude>
                    </excludes>
                    <!--
                    JaCoCo's agent appends to an existing .exec by default, so a
                    run over a target/ left by an earlier build reports the union
                    of both — code whose test was just deleted still counts as
                    covered, and the gate passes on evidence that no longer
                    exists. The documented mitigation used to be "remember to run
                    clean", which is not a mitigation.

                    Safe because both Surefire and Failsafe run a single fork
                    here (forkCount defaults to 1). If that ever changes, each
                    fork would truncate the previous one's data and this must
                    become a per-execution append with a distinct destFile.
                    -->
                    <append>false</append>
                </configuration>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <!--
                    Separate exec file for the *IT run, so unit and integration
                    coverage stay distinguishable and can be merged
                    deterministically below rather than relying on jacoco's
                    append-into-the-same-file default (which is also what let a
                    stale exec silently inflate the reported numbers).
                    -->
                    <execution>
                        <id>prepare-agent-integration</id>
                        <goals>
                            <goal>prepare-agent-integration</goal>
                        </goals>
                        <configuration>
                            <destFile>${project.build.directory}/jacoco-it.exec</destFile>
                            <propertyName>failsafeArgLine</propertyName>
                        </configuration>
                    </execution>
                    <!-- Unit-only report: fast feedback from a plain `mvn test`. -->
                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>merge-all</id>
                        <phase>post-integration-test</phase>
                        <goals>
                            <goal>merge</goal>
                        </goals>
                        <configuration>
                            <fileSets>
                                <fileSet>
                                    <directory>${project.build.directory}</directory>
                                    <includes>
                                        <include>jacoco.exec</include>
                                        <include>jacoco-it.exec</include>
                                    </includes>
                                </fileSet>
                            </fileSets>
                            <destFile>${project.build.directory}/jacoco-merged.exec</destFile>
                        </configuration>
                    </execution>
                    <!--
                    The authoritative report: unit + integration together.
                    Lands in site/jacoco-merged so it never overwrites the
                    unit-only one above.
                    -->
                    <execution>
                        <id>report-merged</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                        <configuration>
                            <dataFile>${project.build.directory}/jacoco-merged.exec</dataFile>
                            <outputDirectory>${project.reporting.outputDirectory}/jacoco-merged</outputDirectory>
                        </configuration>
                    </execution>
                    <execution>
                        <!--
                        Enforces the coverage bar from CLAUDE.md: >=80% overall
                        line AND branch coverage, and 100% line+branch coverage
                        on the pricing, seat-hold, and refund-proration logic
                        specifically, since a silent bug there has direct
                        financial impact.

                        Bound to `verify` and reading the MERGED exec: bound to
                        `test` (as it was), it ran before integration-test and
                        so graded the Surefire suite alone - the *IT classes
                        could not contribute a single covered line to the gate
                        they were supposed to help satisfy.
                        -->
                        <id>check</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                        <configuration>
                            <dataFile>${project.build.directory}/jacoco-merged.exec</dataFile>
                            <rules>
                                <rule>
                                    <element>BUNDLE</element>
                                    <limits>
                                        <limit>
                                            <counter>LINE</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.80</minimum>
                                        </limit>
                                        <limit>
                                            <counter>BRANCH</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.80</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                                <rule>
                                    <element>CLASS</element>
                                    <includes>
                                        <include>com.ticketwave.pricing.service.PricingServiceImpl</include>
                                        <include>com.ticketwave.catalog.service.SeatHoldServiceImpl</include>
                                        <include>com.ticketwave.catalog.scheduling.SeatHoldExpirationScheduler</include>
                                        <include>com.ticketwave.payment.service.RefundPolicyService</include>
                                    </includes>
                                    <limits>
                                        <limit>
                                            <counter>LINE</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>1.00</minimum>
                                        </limit>
                                        <limit>
                                            <counter>BRANCH</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>1.00</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                        <path>
                            <groupId>org.mapstruct</groupId>
                            <artifactId>mapstruct-processor</artifactId>
                            <version>${mapstruct.version}</version>
                        </path>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok-mapstruct-binding</artifactId>
                            <version>${lombok-mapstruct-binding.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
